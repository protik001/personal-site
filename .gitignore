# --- never in the repo ---
node_modules/
.DS_Store
_archive/

# Team-internal (agents, ops, drafts, session rules) and planning docs — live on Protik's machine only
.claude/
ops/
drafts/
CLAUDE.md
docs/
